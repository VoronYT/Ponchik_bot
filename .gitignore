# Файл с секретными переменными
.env

# Папки с кэшем Python
__pycache__/
*.pyc

# Папки виртуального окружения (если вы его создаете внутри проекта)
venv/
.venv/

# Папка с постоянными данными (база данных, сессии),
# которая будет заменена на Volume при деплое.
data/
